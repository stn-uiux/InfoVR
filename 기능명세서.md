# 🏢 3D 서버실 관제 및 관리 시스템 기능 명세서

> **버전**: v1.5  
> **최종 수정일**: 2026-08-19  
> **기술 스택**: React + TypeScript, Three.js (React Three Fiber), Zustand, Vite

본 시스템은 웹 브라우저 환경에서 3D로 구현된 서버실을 직관적으로 모니터링하고, 장비 배치 및 인프라 데이터를 직접 관리할 수 있는 플랫폼입니다. 전체 기능은 현황을 파악하는 **[대시보드(뷰 모드)]**, 인프라를 직접 구성하는 **[수정 모드(에딧 모드)]**, 그리고 장비 하드웨어를 시각적으로 설계하는 **[포트맵핑 마법사]**로 나뉩니다.

---

## 🖥️ 1. 대시보드 (Dashboard / View Mode)

서버실의 전체적인 상황을 모니터링하고, 배치된 랙과 장비의 현재 상태를 조회하는 관제 목적의 모드입니다.

### 1.1 3D 서버실 탐색 및 네비게이션

* **3D 환경 탐색**: 마우스 드래그와 스크롤을 이용해 3D 서버실 화면을 자유롭게 회전, 이동, 확대/축소할 수 있습니다.
* **방향키 카메라 이동**: 키보드 방향키로 카메라 시점을 이동할 수 있습니다. `↑`/`↓` 키는 카메라와 시점 타깃을 Y축 위/아래로 이동하며, `←`/`→` 키는 현재 카메라 기준 좌우 방향으로 평면 이동합니다.
* **랙(Rack) 자동 포커싱**: 화면에 보이는 랙을 마우스로 클릭하면, 카메라가 부드럽게 해당 랙의 정면으로 자동 이동하여 세부 모습을 보여줍니다.
* **랙 네비게이션 캐러셀 (Focus Carousel)**: 뷰 모드 하단에 배치된 캐러셀 UI를 통해 현재 씬 내 모든 랙을 순차적으로 탐색할 수 있습니다. 캐러셀 항목 클릭 시 해당 랙으로 카메라가 자동 이동합니다.
* **전체보기**: 화면 우측의 `전체보기` 버튼을 클릭하면 씬 내 모든 3D 오브젝트가 화면에 최적 비율로 맞춰져 보여집니다.
* **계층 구조 트리 (Hierarchy Tree)**: 화면 좌측의 트리 메뉴를 통해 원하는 구역(예: 특정 사이트 > 특정 룸)을 선택하면, 해당 구역으로 즉시 화면 시점이 이동합니다. 트리의 깊이(Depth)는 고정되어 있지 않고 사용자 환경에 맞춰 자유롭게 구성됩니다.
  * **노드 유형**: `root`, `group`, `site`, `room`, `zone` 5가지 유형을 지원합니다.


### 1.2 인프라 통합 모니터링 위젯

뷰 모드에서 랙을 선택하지 않았을 때 화면 우측에 대시보드 위젯이 표시됩니다.

* **에러 상태 대시보드 (Error Summary)**:
  * 네트워크 및 장비 에러를 위험도(`Critical`, `Major`, `Minor`, `Warning`)별로 분류하여 4개의 통계 카드로 요약 표시합니다.
  * 위험도 카드를 클릭하면 해당 등급의 에러 목록이 하단 테이블에 Drill-down 방식으로 표시됩니다.
  * **에러 항목 클릭 시 자동 포커싱**: 테이블에서 특정 에러 항목을 클릭하면, 해당 에러가 발생한 장비의 랙으로 카메라가 즉시 이동하고, 장비 상세 모달이 해당 포트를 하이라이트한 상태로 열립니다.
  * **크로스-노드 탐색**: 에러가 현재 활성 노드가 아닌 다른 노드에 속한 경우, 자동으로 해당 노드로 전환 후 포커싱됩니다.

* **환경 센서 모니터링 (System Environment Overview)**:
  * 계층 구조에 등록된 각 노드별 온도(°C)와 습도(%)를 날씨 스타일 위젯으로 시각화합니다.
  * 온도 게이지 바는 15°C~35°C 범위로 정규화되며, 27°C 이상은 주황, 31°C 이상은 빨강 그라디언트로 위험도를 직관적으로 나타냅니다.
  * 습도는 물방울 아이콘의 채움 레벨로 시각화됩니다.
  * 노드 행 클릭 시 해당 노드로 씬이 전환됩니다.

### 1.3 랙 및 장비 상세 정보 조회

* **랙 정보 패널 (Device Panel)**: 랙을 클릭하면 우측에 패널이 열리며, 해당 랙의 전체 크기(U 용량), 장착된 장비 리스트, 남은 여유 공간, 랙 이름을 확인할 수 있습니다. 수정 모드에서는 장비의 드래그 앤 드롭 재배치, 등록 장비에서의 드래그 장착이 가능합니다.
* **장비 상세 정보 모달 (Device Modal)**: 장비를 클릭하면 모달이 열리며, IP 주소, MAC 주소, 제조사(Vendor), 모델명, 장비 유형(Switch/Router/Server) 등의 상세 스펙을 조회할 수 있습니다.
* **장비 및 포트 시각화 (SVG 프리뷰)**: 장비 상세 모달 안에는 장비 외관을 표현하는 고해상도 SVG 그래픽이 제공됩니다.
  * **양면 장비 표시**: 모델 등록 시 앞면과 뒷면 SVG가 모두 등록된 장비는 상세 모달에서 앞면과 뒷면을 상하로 함께 표시합니다. 서버 장비처럼 후면 포트가 중요한 장비도 모달 안에서 바로 확인할 수 있습니다.
  * **기본 표시 면 표시**: 장비별로 선택된 기본 표시 면(앞면/뒷면)은 상세 모달 안에서 `기본` 라벨로 표시되며, 랙 장비 텍스처와 장비 목록 썸네일 표시 기준으로도 사용됩니다.
  * **포트 상태 확인 (툴팁)**: 그래픽 상의 각 포트 영역에 마우스를 올리면 포트 번호, 포트 이름, 연결 상태를 즉시 확인할 수 있습니다.
  * **에러 포트 표시**: 에러가 발생한 포트는 그래픽 위에 위험도별 색상 마커(빨강=Critical, 주황=Major, 노랑=Minor, 파랑=Warning)로 표시되어, 어느 위치의 포트에 문제가 생겼는지 직관적으로 알 수 있습니다.
  * **모듈러 카드 포트 연동**: 카드 기반 장비의 경우, 각 포트가 어느 카드 인스턴스에 소속되어 있는지 `cardInstanceId`를 통해 식별됩니다.

---

## 🛠️ 2. 수정 모드 (Edit Mode)

3D 서버실의 레이아웃을 직접 꾸미고, 랙과 장비의 배치 및 세부 스펙을 편집하는 관리 목적의 모드입니다. 상단 툴바의 `Edit Mode: ON/OFF` 토글로 진입합니다.

### 2.1 3D 환경 및 레이아웃 편집

* **가상 공간 (CyberSpace) 구축**:
  * **공간 사이즈 조절**: 우측 '가상 공간' 패널에서 공간의 가로(W), 세로(D) 사이즈를 설정하고 `3D공간 제작` 버튼으로 즉각 공간을 렌더링할 수 있습니다. 
  * **미리보기 가이드 라인**: 편집 모드 시 가상공간의 크기를 직관적으로 가늠할 수 있도록, 바닥과 공간 둘레에 하늘색 와이어프레임 박스가 렌더링됩니다.
  * 환경 설정(사이즈 등) 데이터는 3D 씬과 동기화되어 실시간으로 반영됩니다.
* **오브젝트 직접 배치**: 조작축(기즈모)을 이용해 랙이나 집기들을 마우스로 잡고 자유롭게 이동, 회전, 크기 조절을 할 수 있습니다. 각 모델은 개별적으로 잠금/해제(🔓/🔒) 토글이 가능합니다.
* **기본 제공 모델 활용**: 다음의 3D 모델이 기본으로 제공되어 서버실 환경을 구성할 수 있습니다:
  * **벽(Wall)**: 높이, 길이, 두께, 색상을 파라메트릭으로 조절 가능
  * **파티션(Partition)**: 높이, 길이, 두께, 색상, 가시성 모드(투명 유리/불투명) 선택 가능
  * **의자(Chair), 책상(Desk, Desk2)**: 씬 분위기 구성용
  * **시계(Clock)**: 배치 시 3D 모델 내에서 실제 현재 시간이 실시간으로 표시됩니다
  * **조명(Light)**: 강도(intensity), 색상, 그림자 여부, 그림자 해상도 조절 가능
* **사용자 정의 모델 추가**: 사용자가 보유한 3D 모델 파일(`.glb` 또는 `.gltf`)을 파일 선택 또는 **드래그 앤 드롭**으로 업로드하여 서버실 내 원하는 위치에 마음대로 배치할 수 있습니다.
* **모델 속성 편집 패널**: 좌측 패널에서 모델을 선택하면 위치(X/Y/Z), 회전(X/Y/Z), 크기(X/Y/Z) 및 모델별 고유 파라미터를 상세 편집할 수 있습니다.
* **씬 오브젝트 관리**: 좌측 `Scene Objects` 리스트에서 배치된 모든 모델의 목록을 확인하고, 선택, 잠금/해제, 삭제를 관리할 수 있습니다.
* **3D 모델 내보내기/불러오기**: 배치한 모든 3D 모델 데이터(위치, 회전, 크기, 바이너리 포함)를 JSON 파일로 내보내기(Save)하거나, 기존 파일을 불러오기(Load)하여 환경을 복원할 수 있습니다.

### 2.2 랙(Rack) 관리

* **랙 생성 및 삭제**: 24U, 32U, 48U 크기의 표준(Std) 및 와이드(Wide) 규격의 빈 랙을 공간에 새롭게 추가하거나, 불필요해진 랙을 삭제할 수 있습니다.
* **랙 방향 설정**: 3D 씬에서 랙을 클릭한 뒤, 우측 패널에서 랙이 바라보는 방향(0° / 90° / 180° / 270°)을 변경할 수 있습니다.
* **랙 이름 변경**: 랙 패널에서 랙 타이틀을 직접 편집할 수 있습니다.

### 2.3 통합 장비 관리 모달 (등록/수정/삭제 및 데이터 연동)

별도의 '통합 장비 관리 모달' 창을 통해 장비의 데이터베이스와 상세 스펙을 일괄적으로 제어합니다.

* **계층 노드 관리 (좌측 영역)**: 장비가 소속될 그룹이나 룸 같은 계층(트리 구조) 노드를 직접 생성, 수정, 삭제할 수 있습니다.
* **장비 검색 및 관리 (우측 영역)**: 등록된 전체 장비를 검색하거나 장비 테이블 리스트에서 세부 정보를 수정 및 삭제할 수 있습니다.
  * **삭제 확인 모달**: 장비가 특정 랙에 배치되어 있는 경우, 삭제 시 해당 사실을 경고하고 확인을 요구합니다.
* **장비 등록 및 모듈(하드웨어) 구성**:
  * 새로운 장비를 등록할 때 원하는 모델을 선택합니다.
  * 카드 확장이 가능한 장비(모듈러 장비)의 경우, **장비 조립 모달(Equipment Assembly Modal)**을 통해 장비 구성을 직접 세팅할 수 있습니다.
  * 동시에 제공되는 장비 그래픽(프리뷰) 화면에서 비어있는 포트를 클릭하여 Ethernet(이더넷)이나 SFP 모듈을 직접 꽂거나 빼는 등 세부 하드웨어 스펙을 완성할 수 있습니다.
  * 모델에 앞면/뒷면 SVG가 모두 등록되어 있으면 장비 등록 프리뷰에서도 양면이 함께 표시되며, 랙과 장비 목록에서 기본으로 보여줄 면(앞면/뒷면)을 장비별로 선택할 수 있습니다.
* **장비 목록 엑셀 연동**: 장비 목록에서 원하는 항목을 체크하여 엑셀 파일(`.xlsx`)로 내보내기(Export)를 할 수 있으며, 기존 장비 데이터가 정리된 외부 엑셀 파일을 불러와(Import) 한 번에 대량 등록할 수 있습니다.

#### 2.3.1 장비 조립 워크플로우 (Equipment Assembly Modal)

장비 조립 모달은 모듈러 장비의 카드 구성과 포트 자동 생성을 담당하는 전용 UI입니다. 통합 장비 관리 모달에서 장비 등록/수정 시 진입합니다.

* **Step 1 — 장비 모델 선택**: 모달 진입 시 등록된 장비 모델 목록이 그리드 형태로 제공됩니다. 각 모델 카드에는 모델명과 슬롯 구조 요약(열×행, Mixed, Row-based 등)이 표시됩니다.
* **Step 2 — 카드 삽입 (슬롯 배치)**:
  * 모델을 선택하면 장비 외관의 **원본 SVG 그래픽**이 캔버스 중앙에 렌더링되고, 카드를 삽입할 수 있는 **슬롯**이 점선 오버레이로 표시됩니다.
  * 좌측 **카드 라이브러리 사이드바**에서 삽입할 카드를 선택한 뒤, 캔버스의 빈 슬롯을 클릭하면 해당 위치에 카드가 즉시 삽입됩니다.
  * 카드 라이브러리는 그룹별로 분류되어 표시됩니다: `CPIOM 카드`, `Standard 카드`, `IXR 카드` 등.
  * 삽입 불가능한 슬롯(이미 점유되었거나 크기가 맞지 않는 경우)에는 **시각적 Dim 처리**와 함께 경고 토스트가 표시됩니다.
  * 삽입 가능한 슬롯은 **하이라이트 애니메이션**으로 강조되어 직관적으로 안내합니다.
  * 삽입된 카드는 슬롯 위에 **인라인 SVG**로 실시간 렌더링되며, 마우스 호버 시 `×` 버튼으로 개별 제거할 수 있습니다.
* **슬롯 레이아웃 유형**: 장비 모델에 따라 3가지 슬롯 레이아웃이 자동 적용됩니다:
  * **Uniform Grid**: 고정 열×행 그리드. Half/Full 카드 너비를 지원하며, Full 카드는 한 행 전체를 차지합니다.
  * **Mixed Layout**: 모델별로 명시적으로 정의된 슬롯 위치·크기. 같은 행의 Full/Half 슬롯이 상호 배제되며, `allowedCardGroups`를 통해 CPIOM 전용 슬롯 등 카드 그룹 제한이 적용됩니다.
  * **Row-based Layout**: 행(Row) 단위로 서브 슬롯이 정의된 구조. 행 라벨(R1, R2…)이 표시되며, 카드 SVG 너비가 서브 슬롯 너비를 초과하면 삽입이 차단됩니다.
* **Step 3 — 저장 및 자동 생성**:
  * `저장` 버튼 클릭 시 다음이 자동 수행됩니다:
    1. **썸네일 자동 합성**: 베이스 장비 SVG 위에 삽입된 카드 SVG를 좌표 기반으로 합성한 뒤, Canvas API를 통해 **WebP 이미지**로 변환합니다. 이 썸네일은 3D 뷰어의 장비 텍스처와 대시보드 미리보기에 사용됩니다.
    2. **포트 맵 자동 생성**: 각 카드 SVG 내부의 `port-hitbox` 요소를 파싱하여 포트 목록(`GeneratedPort`)을 자동 생성합니다. 포트 번호는 `shelfNo/slotNo/localPort` 형식(예: `1/1/9`)으로 자동 부여됩니다.
  * `전체 제거` 버튼으로 모든 삽입 카드를 일괄 초기화할 수 있습니다.

#### 2.3.2 장비 모델 관리 (Model Registration Modal)

장비 모델 관리 모달은 시스템에서 사용할 장비 모델의 **템플릿**을 정의하는 전용 UI입니다. 등록된 모델은 장비 조립(2.3.1) 시 모델 선택 목록에 포함되며, 장비 등록 시 해당 모델의 SVG 그래픽과 카드 구성 정보가 자동으로 적용됩니다. 수정 모드 툴바에서 `장비 모델 관리` 버튼으로 진입합니다.

* **탭 구조**: 모달은 두 개의 탭으로 구성됩니다:
  * **새 모델 등록 탭**: 새로운 장비 모델을 등록하거나 기존 모델을 수정하는 폼
  * **등록된 모델 탭**: 현재 등록된 모든 모델(기본 + 커스텀)의 리스트를 조회 및 관리. (기존 탭 형식에서 직관적인 세로 리스트 형태로 개편)

##### 새 모델 등록 폼

* **기본 정보 (필수)**:
  * **모델명**: 장비 모델의 고유 이름 입력 (예: `7250 IXR-6`)
  * **Rack Unit (U)**: 장비가 차지하는 랙 유닛 수 설정 (1U~48U 범위)
  * **표시 이름 자동 생성**: 모델명과 U 값을 조합하여 `[{unit}U] {modelName}` 형식의 표시 이름이 실시간으로 프리뷰됩니다 (예: `[4U] 7250 IXR-6`)

* **모델 타입 선택**:
  * **일반 장비 (Normal)**: 단일 바디로 구성된 장비. 모델 SVG 파일 1개만 필요합니다.
  * **카드 기반 장비 (Card-based)**: 카드 슬롯이 있어 확장 카드를 삽입할 수 있는 모듈러 장비. 모델 SVG 외에 기본 섀시 SVG와 카드 영역 좌표 설정이 추가로 필요합니다.

* **모델 SVG 파일 업로드 (필수)**:
  * 장비의 전면부 외관을 표현하는 SVG 파일을 업로드합니다.
  * SVG 파일만 지원되며, 업로드 시 파일명이 표시됩니다.
  * 업로드된 SVG는 즉시 프리뷰로 렌더링되어 시각적으로 확인할 수 있습니다.
  * SVG 내부의 `viewBox` 또는 `width`/`height` 속성에서 장비 크기(px)가 자동 추출됩니다.
  * **앞면/뒷면 이미지 사용**: 체크박스를 활성화하면 전면 SVG와 별도로 후면 SVG를 추가 업로드할 수 있습니다. 후면 포트 확인이 중요한 서버 장비나 양면 관리가 필요한 장비 모델에 사용합니다.
  * **기본 표시 면 선택**: 양면 이미지가 등록된 모델은 `앞면` 또는 `뒷면` 중 랙 장비 텍스처와 장비 목록 썸네일에서 기본으로 표시할 면을 선택할 수 있습니다. 선택값은 장비 등록 시 초기 기본값으로 반영됩니다.

* **카드 기반 장비 추가 설정** (모델 타입이 `카드 기반`일 때만 표시):
  * **기본 섀시 SVG 업로드**: 카드가 삽입되지 않은 빈 장비 바디의 SVG 파일을 별도로 업로드합니다. 이 SVG는 장비 조립 시 카드 삽입 캔버스의 배경으로 사용됩니다.
  * **인터랙티브 섀시 그리드 생성**: 업로드된 섀시 SVG 위에서 마우스를 직접 드래그하여 카드 영역을 그릴 수 있습니다. 기존 좌표 수치 폼 입력 방식이 완전히 제거되었으며 반응형 섀시 그리드가 자동 생성됩니다.
  * **엣지 핸들 리사이징**: 생성된 그리드의 4면 가장자리 및 모서리 엣지 핸들을 마우스로 드래그하여 영역 크기를 실시간으로 확대/축소할 수 있습니다.
  * **디바이더 리사이징 (행/열 크기 조절)**: 내부 격자선(수평선 및 수직선)을 드래그하여 개별 행 높이와 열 너비를 세밀하게 조절할 수 있습니다.
  * **셀 다중 선택 및 병합/나누기 툴바**: `Shift + 클릭`으로 여러 셀을 선택한 뒤 에디터 하단의 툴바를 이용해 셀을 병합하거나 나눌 수 있습니다.
  * **행간 간격(Gap) 컨텍스트 메뉴**: 그리드 셀을 마우스 우클릭하면 나타나는 커스텀 컨텍스트 메뉴를 통해 해당 행 하단에 적용할 간격(px)을 직접 입력합니다.
    * **양수 간격 (Positive Gap)**: 입력 시 주황색(Warning 스타일) 점선 박스와 `간격 {gap}px` 오버레이가 렌더링됩니다.
    * **음수 간격 (Negative Gap / 겹침)**: 입력 시 빨간색(Error/Danger 스타일) 점선 박스와 `겹침 {gap}px` 오버레이가 렌더링되어 다음 행이 위로 올라오는 겹침 효과를 시각적으로 피드백합니다.

* **타입(변형) 관리 — 카드 기반 장비 전용**:
  * 동일한 섀시(Base Chassis) 위에 **다른 카드 구성을 가진 여러 변형(Variant/Type)**을 정의하고 관리할 수 있습니다.
  * **타입 추가**: `+ 새 타입 추가` 버튼을 클릭하면 장비 조립 모달(Equipment Assembly Modal)이 열리며, 해당 섀시에 카드를 삽입하여 새로운 타입을 구성합니다.
  * **타입명 설정**: 각 타입에 고유한 이름(예: `기본타입`, `풀옵션`)을 부여합니다.
  * **중복 타입명 방지**: 동일한 모델 내에 같은 이름의 타입이 이미 존재하면 경고 메시지를 표시하고 저장을 차단합니다.
  * **타입별 썸네일 자동 생성**: 타입을 저장할 때마다 해당 카드 구성의 합성 SVG를 기반으로 **PNG 썸네일이 자동 생성**되어 모델 목록에 즉시 반영됩니다.
  * **기본타입 썸네일 우선**: `기본타입`이라는 이름의 타입이 존재하면, 모델 목록의 썸네일이 반드시 해당 기본타입의 합성 이미지로 표시됩니다.
  * **타입 수정/삭제**: 등록된 타입 목록에서 수정(✏️) 또는 삭제(🗑️) 버튼으로 개별 관리합니다.
  * **프로젝트 영속화**: 타입 및 섀시 영역 데이터는 `customModels.json` 파일에 저장되어 **Git을 통해 프로젝트와 함께 버전 관리**됩니다. 브라우저 로컬 스토리지가 아닌 프로젝트 소스 코드의 일부로 유지되므로, 팀 구성원 간 공유가 가능합니다.

* **카드 할당** (카드 기반 장비 전용):
  * 해당 모델에서 사용 가능한 카드 목록을 정의합니다.
  * **새 카드 등록**: 서브 모달(카드 등록 폼)을 열어 커스텀 카드를 새로 등록하고 즉시 할당합니다.
  * **기존 카드에서 선택**: 시스템에 등록된 모든 카드(빌트인 + 커스텀)를 그리드로 표시하여 토글 선택/해제할 수 있습니다. 각 카드에는 SVG 프리뷰, 카드명, 폭 타입(`HALF`/`FULL`), 크기(px)가 표시됩니다.
  * 할당된 카드 목록에서 `×` 버튼으로 개별 할당 해제가 가능합니다.

* **폼 액션 버튼**:
  * **모델 등록**: 입력된 정보로 새 모델을 저장합니다. 필수 필드 미입력 시 인라인 에러 메시지가 표시됩니다.
  * **모델 수정**: 기존 모델 편집 시 버튼이 `모델 수정`으로 변경됩니다. 원래 등록일시(`createdAt`)는 유지됩니다.
  * **신규 등록으로 전환**: 수정 모드에서 폼을 초기화하고 신규 등록 모드로 돌아갑니다.
  * **취소**: 폼을 초기화하고 모달을 닫습니다.

##### 등록된 모델 관리

* **기본 장비 모델 목록**: 시스템에 내장된 기본 장비 모델(18종)이 리스트로 표시됩니다. 각 항목에는 다음 정보가 포함됩니다:
  * SVG 썸네일 (축소 렌더링) — 기본타입이 등록된 경우 해당 합성 이미지가 우선 표시
  * 표시 이름 (`[{unit}U] {modelName}`)
  * 모델 타입 태그 (`고정형` / `섀시형`)
  * 제조사, 장비 유형, 타입 수
  * 수정 완료 여부 뱃지 (`수정됨`)
  * **수정** 버튼: 기본 모델의 SVG, 카드 영역, 타입을 커스터마이징할 수 있습니다.
  * **숨기기/초기화** 버튼: 미수정 모델은 목록에서 숨길 수 있고, 수정된 모델은 수정 내용을 초기화합니다.

* **숨김 처리된 기본 모델 섹션**: 숨긴 기본 모델은 별도의 `숨김 처리된 기본 모델` 섹션에 표시되며, **복구 버튼(🔄)**을 클릭하여 언제든지 원래 목록으로 복원할 수 있습니다.

* **커스텀 모델 목록**: 사용자가 직접 등록한 모델이 별도 섹션에 표시됩니다. 각 항목에는 다음 정보가 포함됩니다:
  * SVG 썸네일 (축소 렌더링)
  * 표시 이름 (`[{unit}U] {modelName}`)
  * 랙 유닛 수, SVG 원본 크기(px)
  * 모델 타입 태그 (`고정형` / `섀시형`)
  * 카드 기반 장비의 경우 할당된 카드 수 및 타입 수
  * 양면 모델의 경우 양면 등록 여부와 기본 표시 면(`앞면 기본` / `뒷면 기본`)
* **모델 수정**: `✏️ 수정` 버튼 클릭 시 해당 모델의 데이터가 등록 폼에 로드되어 편집할 수 있습니다.
* **모델 삭제**: `×` 버튼으로 모델을 즉시 삭제합니다.
* **빈 상태 안내**: 등록된 모델이 없을 경우 "등록된 모델이 없습니다" 안내 메시지와 함께 등록 탭 이동을 유도합니다.
* **토스트 알림**: 모델 등록/수정/삭제 시 결과를 토스트 메시지로 피드백합니다 (3초간 표시).

#### 2.3.3 카드 등록 (Card Registration Form)

모델 등록 모달 내에서 새로운 커스텀 카드를 등록하는 서브 모달입니다. 등록된 카드는 장비 모델에 할당하거나, 장비 조립 시 카드 라이브러리에서 선택하여 슬롯에 삽입할 수 있습니다.

* **카드명 (필수)**: 카드의 이름 입력 (예: `R-series-custom`)
* **카드 폭 타입**: `Half (1열)` 또는 `Full (2열)` 중 선택. Half는 슬롯 1열을 차지하고, Full은 2열 전체 전체를 차지합니다.
* **카드 SVG 파일 업로드 (필수)**: 카드 외관의 SVG 파일을 업로드합니다. 업로드 시 SVG 내부에서 원본 크기(`width × height`)가 자동 추출되어 표시되며, SVG 프리뷰가 렌더링됩니다.
* **저장 시 자동 처리**: 카드 등록 완료 시 고유 `cardId`가 자동 생성되고, 등록일시가 기록됩니다. 등록된 카드는 즉시 현재 편집 중인 모델의 할당 카드 목록에 추가됩니다.

### 2.4 데이터 상태 저장 및 연동

* **전체 환경 내보내기 및 불러오기**: 개별 장비 목록뿐만 아니라, **3D 서버실 내의 모든 랙 위치, 장비 배치 상태, 계층 노드 구성 등 전체 환경 데이터를 엑셀 파일 형태로 한 번에 내보내거나, 백업된 파일을 불러와 전체 인프라 환경을 동일하게 복원**할 수 있습니다.
* **실행 취소 및 다시 실행 (Undo/Redo)**: 수정 모드에서 작업한 모든 위치 변경이나 배치 이력은 단축키(`Ctrl+Z` / `Ctrl+Shift+Z`, macOS에서는 `⌘+Z` / `⌘+Shift+Z`)를 통해 이전 상태로 되돌리거나 다시 적용할 수 있습니다.
* **변경 사항 알림 및 저장**: 작업 중 레이아웃이나 설정 변경 사항이 감지되면 `Save` 버튼이 활성화되고, 단축키(`Ctrl+S` / `⌘+S`)나 저장 버튼을 눌러 데이터를 안전하게 영구 저장할 수 있습니다.
* **저장되지 않은 변경 알림 (Unsaved Changes Dialog)**: 저장되지 않은 변경 사항이 있는 상태에서 페이지를 이탈하려 할 경우, 경고 다이얼로그가 표시됩니다.
* **로컬 스토리지 영속화**: 모든 환경 데이터는 브라우저의 `localStorage` 및 `IndexedDB`에 자동 저장되며, 브라우저를 닫았다 열어도 마지막 상태가 유지됩니다.
* **프로젝트 레벨 영속화**: 장비 모델의 커스텀 설정(섀시 영역, 타입/변형)은 `customModels.json` 파일에 저장되어 프로젝트 소스 코드의 일부로 Git 관리됩니다.
* **데이터 초기화**: `초기화` 버튼을 통해 저장 데이터를 삭제하고 시스템을 초기 상태로 되돌릴 수 있습니다. 단, 장비 모델 관리 데이터(섀시 영역, 타입)는 초기화 대상에서 제외되어 보호됩니다.
* **샘플 데이터 로드**: `Sample` 버튼을 통해 미리 구성된 샘플 랙/장비/노드 데이터를 즉시 불러와 시스템을 시연할 수 있습니다.

---

## 🔌 3. 포트맵핑 마법사

장비의 외관 이미지 위에 포트 영역을 시각적으로 매핑하고, **AI 비전 분석** 또는 **수동 편집**을 통해 네트워크 장비의 포트 레이아웃을 정밀하게 설계하는 독립적인 페이지입니다. 수정 모드 툴바에서 `포트맵핑 마법사` 버튼 또는 `/port-wizard` 경로로 진입합니다.

### 3.1 이미지 업로드 및 AI 자동 분석

* **장비 이미지 업로드**: 네트워크 장비의 전면부 사진(PNG, JPG, WEBP)을 파일 선택 또는 드래그 앤 드롭으로 업로드합니다.
* **SVG 파일 임포트**: SVG 파일을 업로드하면 내부의 `<path>`, `<rect>`, `<polygon>`, `<circle>` 등 벡터 요소를 자동 파싱하여 포트 영역으로 변환합니다. 이전에 포트맵핑 마법사에서 내보낸 SVG(`data-local-port`, `port-hitbox` 속성 포함)의 경우 포트 이름/번호까지 자동 복원됩니다.
* **Gemini AI 자동 포트 분석 (Neural Analysis)**: 이미지 업로드 후 `Initiate Neural Analysis` 버튼을 클릭하면, **Gemini 3 Flash** 비전 AI가 장비 이미지를 분석하여 모든 물리적 포트(Ethernet/RJ45, SFP, SFP+, Console, USB, Management 등)를 자동으로 감지합니다.
  * AI는 각 포트의 **정밀한 바운딩 박스 좌표**(0~1000 정규화 스케일)와 **포트 이름/번호**를 반환합니다.
  * 분석 진행 중 터미널 스타일의 **실시간 로그**와 **프로그레스 바**가 표시됩니다.
  * 분석 실패 시 에러 메시지와 함께 `Retry` 버튼이 제공됩니다.

### 3.2 포트 편집 모드 (Edit Mode)

이미지 위에 오버레이된 포트 박스를 직접 편집하는 인터랙티브 모드입니다. 상단 툴바의 `Lock Layout` / `Unlock Editor` 버튼으로 토글합니다.

#### 3.2.1 포트 박스 조작

* **드래그 이동**: 포트 박스를 마우스로 잡고 자유롭게 이동할 수 있습니다. 여러 포트를 선택한 상태에서 드래그하면 일괄 이동됩니다.
* **리사이즈**: 포트 박스 우하단의 리사이즈 핸들을 드래그하여 크기를 조절합니다.
* **키보드 미세 조정**: 선택된 포트를 방향키(↑↓←→)로 1px씩 이동하며, `Shift`를 함께 누르면 10px씩 이동합니다.
* **수동 포트 추가**: 상단 `Manual Port` 버튼으로 이미지 중앙에 새 포트 박스를 수동 생성합니다. 포트 번호는 자동으로 다음 시퀀스가 부여됩니다.
* **포트 삭제**: 포트 컨트롤 패널의 `X` 버튼 또는 `Delete`/`Backspace` 키로 선택된 포트를 삭제합니다.

#### 3.2.2 포트 속성 편집

각 포트를 선택하면 해당 포트 근처에 인라인 속성 편집 패널이 표시됩니다:

* **Tag (포트 이름)**: `ethernet`, `sfp`, `mgmt`, `console` 등 포트 유형 이름 설정
* **Seq (포트 번호)**: 포트의 시퀀스 번호 설정
* **Icon (아이콘 유형)**: 포트 박스 위에 표시할 아이콘을 `None` / `Ethernet` / `SFP` 중 선택 (실제 Ethernet·SFP SVG 아이콘이 오버레이됨)
* **W / H (너비/높이)**: 포트 박스의 정규화 좌표 기준 너비·높이를 수치로 직접 입력하거나 `+`/`-` 스텝 버튼으로 1px 단위 미세 조절

#### 3.2.3 다중 선택 및 정렬

* **다중 선택**: `Shift`/`Ctrl`/`⌘` + 클릭으로 개별 추가/제거 선택, 또는 `Ctrl`/`⌘` + 드래그로 영역 선택(Lasso)이 가능합니다.
* **정렬 도구 (2개 이상 선택 시)**: 다중 선택 시 포트 그룹 위에 정렬 팝업이 표시되며, 다음 8가지 정렬·분배 작업을 수행합니다:
  * 상단/중앙/하단 정렬 (Align Top / Vertical Center / Align Bottom)
  * 좌측/중앙/우측 정렬 (Align Left / Horizontal Center / Align Right)
  * 수평 균등 분배 (Distribute Horizontally)
  * 수직 균등 분배 (Distribute Vertically)
* **일괄 속성 편집**: 다중 선택 상태에서 Tag, Width, Height를 일괄 변경할 수 있습니다.

#### 3.2.4 포트 복제

* **포트 복제 (`Ctrl+D` / PRT 버튼)**: 선택된 포트를 개별적으로 복제합니다. 복제 시 포트 번호는 기존 시퀀스의 빈 번호를 자동으로 찾아 부여되며, 위치는 원본에서 20px 오프셋됩니다.
* **그룹 복제 (`Ctrl+G` / GRP_CPY 버튼)**: 선택된 포트 그룹 전체를 동일한 포트 이름 범주 내에서 복제합니다. 이미 사용 중인 번호를 건너뛰고 다음 가용 번호를 자동 할당합니다.

### 3.3 캔버스 네비게이션

* **줌 (확대/축소)**: 마우스 휠 또는 하단 줌 슬라이더(10%~500%)로 이미지를 확대/축소합니다. 핀치 줌도 지원됩니다.
* **FIT 버튼**: 이미지를 캔버스에 최적 비율로 맞춥니다(줌 100% 리셋).
* **패닝**: 이미지 영역에서 마우스 드래그로 자유롭게 스크롤/패닝합니다.

### 3.4 오브젝트 레지스트리 (Object Registry)

화면 우측 사이드바에서 매핑된 모든 포트를 계층적으로 관리합니다.

* **카테고리 그룹핑**: 포트 이름(Tag)별로 자동 분류되어 `ethernet (24 units)`, `sfp (4 units)` 같은 형태로 그룹 표시됩니다.
* **접기/펼치기**: 각 카테고리를 접거나 펼칠 수 있으며, 상단 토글로 전체 일괄 접기/펼치기가 가능합니다.
* **포트 상세 정보**: 각 항목에 포트 번호 뱃지, 포트 이름, X/Y 좌표가 표시됩니다.
* **캔버스 연동**: 레지스트리 항목을 클릭하면 해당 포트가 캔버스에서 선택·하이라이트되고, 마우스를 올리면 캔버스의 해당 포트가 활성 상태로 강조됩니다.

### 3.5 SVG 내보내기 (Export)

* **SVG 파일 다운로드**: 상단 툴바의 `Export` 버튼 클릭 시 파일명 입력 모달이 열립니다.
* **출력 형식**: 원본 이미지를 `<image>` 태그로 배경에 포함하고, 각 포트 영역을 `<path>` 태그(`class="port-hitbox"`, `data-port-type`, `data-local-port` 속성 포함)로 정의한 표준 SVG 파일이 생성됩니다.
* **재임포트 호환**: 내보낸 SVG 파일을 다시 포트맵핑 마법사에 업로드하면 포트 매핑이 완벽하게 복원됩니다.
* **3D 뷰어 연동**: 내보낸 SVG 파일은 본 시스템의 장비 상세 모달(Device Modal)에서 포트 시각화 프리뷰로 직접 사용됩니다.

### 3.6 데이터 영속화

* **자동 저장**: 이미지, 포트 데이터, 분석 결과는 `IndexedDB`(localforage)에 자동 저장되며, 브라우저를 닫았다 열어도 마지막 작업 상태가 복원됩니다.
* **Undo/Redo 히스토리**: 모든 포트 편집 작업(이동, 리사이즈, 삭제, 복제, 정렬 등)에 대해 Undo/Redo가 지원됩니다.
* **프로젝트 초기화**: `Clear Project` 버튼으로 현재 이미지와 모든 포트 데이터를 초기화합니다.

### 3.7 Process Telemetry (프로세스 텔레메트리)

캔버스 하단에 위치한 상태 패널로, 현재 시스템 상태를 표시합니다:

* **대기 상태**: 이미지 미업로드 시 `Null Vector Array` 상태 표시
* **분석 준비**: 이미지 업로드 후 AI 분석 대기 상태 + `Initiate Neural Analysis` 액션 버튼
* **분석 진행 중**: 터미널 스타일 실시간 로그 + 메모리 할당 프로그레스 바
* **분석 완료**: AI가 반환한 장비 기술 설명(Technical Summary) 표시

---

## 🌌 4. arcVRoom (독립형 3D 공간 에디터)

arcVRoom은 서버실 관제 목적 외에 범용적인 3D 가상 공간이나 가구/집기를 자유롭게 배치하고 렌더링할 수 있는 **독립형 3D 씬 에디터** 페이지입니다. 라우팅 경로 `/arcVRoom`으로 진입하여 별도의 앱처럼 동작합니다.

### 4.1 독립적인 3D 환경 제어
* **조명 및 렌더링 효과**: 앰비언트 라이트(Ambient Light) 외에도 다중 조명을 추가/제어하고, 그림자, 블룸(Bloom) 강도, 비네팅(Vignette) 효과 등 후처리(Post-processing) 렌더링 옵션을 제공합니다.
* **배경 및 그리드**: 3D 배경(Environment) 표시 토글, 배경색상 변경, 그리드 가시성 토글 기능을 지원합니다.

### 4.2 에셋 배치 및 조작 (Gizmo)
* **다중 선택 및 그룹화**: 여러 에셋을 동시에 선택하여 한 번에 이동하거나 크기를 조절할 수 있는 그룹핑 기능을 제공합니다.
* **Translate / Rotate / Scale 기즈모**: 객체 선택 시 제공되는 3D 트랜스폼 컨트롤러를 통해 세밀한 좌표/회전/크기 튜닝이 가능합니다.

### 4.3 데이터 최적화 및 압축
* **GLB/GLTF 파일 지원 및 Draco 압축**: 고해상도 3D 모델 파일을 자유롭게 Import/Export 할 수 있으며, Draco 압축된 3D 모델을 원활하게 디코딩하여 성능 저하 없이 로드합니다.

---

## 🎨 5. UI/UX 공통 기능

### 5.1 테마 및 사이드바 상호작용

* **다크/라이트 모드 전환**: 좌측 상단의 테마 토글 버튼으로 다크 모드와 라이트 모드를 즉시 전환할 수 있습니다. 전환 시 3D 환경(CyberSpace) 또한 주간/야간 모드로 완벽하게 연동됩니다.
* **접이식 사이드바 (Collapsible Sidebar)**: 
  * 편집 모드 및 대시보드 화면에서 사이드바 패널의 폭을 조절하거나 아이콘 뷰로 최소화(Fold)할 수 있습니다.
  * **플로팅 아이콘 패널**: 사이드바가 접힌 상태에서 아이콘(계층, 가상 공간, 프로젝트 에셋 등)을 클릭하면, 해당 버튼의 높이(Y축)에 맞춰 **플로팅 패널**이 자연스럽게 오버레이됩니다. 패널의 높이는 내부에 있는 컨텐츠 분량에 맞춰 자동으로 최적화(Auto Fit)됩니다.
* **타이포그래피 및 폰트**: 전역적으로 `Pretendard` 서체가 기본 적용되어 있으며, 포트 매핑 마법사(포트맵핑 마법사) 등 서브 앱까지 일관된 폰트를 적용해 가독성을 크게 개선했습니다.

### 5.2 알림 및 피드백

* **토스트 알림 (Toast)**: 저장 성공, 에러 발생 등 주요 액션 결과를 화면 하단 토스트 메시지로 즉시 피드백합니다. 성공/경고 등 유형에 따라 아이콘과 색상이 구분됩니다.

### 5.3 키보드 단축키

#### 3D 뷰어 (메인 앱)

| 단축키 | 기능 |
|---|---|
| `Ctrl+Z` / `⌘+Z` | 실행 취소 (Undo) |
| `Ctrl+Shift+Z` / `⌘+Shift+Z` | 다시 실행 (Redo) |
| `Ctrl+S` / `⌘+S` | 변경 사항 저장 |

#### 포트맵핑 마법사

| 단축키 | 기능 |
|---|---|
| `Ctrl+Z` / `⌘+Z` | 실행 취소 (Undo) |
| `Ctrl+Y` / `Ctrl+Shift+Z` / `⌘+Shift+Z` | 다시 실행 (Redo) |
| `Ctrl+D` / `⌘+D` | 선택 포트 복제 (Port Copy) |
| `Ctrl+G` / `⌘+G` | 선택 포트 그룹 복제 (Group Copy) |
| `Delete` / `Backspace` | 선택 포트 삭제 |
| `↑` `↓` `←` `→` | 선택 포트 1px 이동 |
| `Shift` + `↑` `↓` `←` `→` | 선택 포트 10px 이동 |
| `Shift`/`Ctrl`/`⌘` + 클릭 | 다중 선택 추가/제거 |
| `Ctrl`/`⌘` + 드래그 | 영역 선택 (Lasso) |

> **참고**: 입력 필드(input, textarea)에 포커스가 있는 경우 단축키가 비활성화되어 일반 타이핑과 충돌하지 않습니다.

---

## 📋 6. 데이터 모델 요약

### 6.1 핵심 엔티티

| 엔티티 | 설명 | 주요 식별자 |
|---|---|---|
| `HierarchyNode` | 계층 트리 노드 (root/group/site/room/zone) | `nodeId` |
| `Rack` | 랙 (3D 위치 + 장비 포함) | `rackId`, `mapId`(소속 노드) |
| `Device` | 랙에 배치된 장비 인스턴스 | `itemId`, `rackId` |
| `RegisteredDevice` | 장비 마스터 인벤토리 (t_device) | `deviceId` |
| `PortState` | 개별 포트 상태 | `portId` |
| `ImportedModel` | 사용자 추가 3D 모델 | `id` |
| `CustomEquipmentModel` | 사용자 등록 장비 모델 템플릿 | `modelId` |
| `CustomCardDefinition` | 사용자 등록 카드 정의 | `cardId` |
| `InsertedCard` | 장비에 삽입된 카드 인스턴스 | `instanceId` |
| `InsertedModule` | 포트에 삽입된 모듈 인스턴스 | `portId`, `hitboxId` |
| `ModelVariant` | 장비 모델의 타입/변형 정의 | `variantId`, `variantName` |

### 6.2 장비 분류

* **장비 유형(DeviceType)**: `Switch`, `Router`, `Server`
* **제조사(VendorName)**: `코위버PTN`, `CISCO`, `Huawei`, `Nokia`, `유비쿼스`
* **랙 사이즈(RackSize)**: `24U`, `32U`, `48U`
* **랙 규격**: 표준(Standard), 와이드(Wide)
* **장비 표시 면(EquipmentViewSide)**: `front`, `rear`
  * `front`: 장비 앞면 SVG 또는 기본 정면 이미지
  * `rear`: 사용자 등록 모델의 뒷면 SVG
  * `defaultViewSide`: 랙 장비 텍스처와 장비 목록 썸네일에서 기본으로 표시할 면
  * `rearSvgRaw`: 커스텀 장비 모델에 저장되는 뒷면 SVG 원본 문자열

---

## ⚙️ 7. 비기능 요구사항

### 7.1 성능 및 안정성

* **Lazy Loading**: `ImportExportModal`, `ModelImporter`, `DeviceRegistrationModal` 등 대형 컴포넌트는 `React.lazy`를 통한 코드 분할로 초기 로딩 성능을 최적화합니다.
* **선택적 리렌더링 방지**: Zustand 스토어에서 개별 셀렉터(`useStore((s) => s.field)`)와 `useShallow`를 사용하여 불필요한 리렌더링을 억제합니다.
* **3D 렌더링 최적화**: 기즈모, 에러 마커 등 3D 오버레이는 선택 시에만 마운트됩니다.
* **글로벌 포커스 조명 사전 마운트**: 랙 선택 시 발생하던 3~5초 UI 프리즈를 해결하기 위해, 포커스 조명(`pointLight`)을 씬에 항상 마운트하고 `intensity`만 동적으로 토글하는 방식으로 Three.js 셰이더 재컴파일을 방지합니다.
* **3D 모델 로딩 에러 격리 (GltfErrorBoundary)**: 사용자가 업로드한 `.glb`/`.gltf` 모델의 로딩이 실패하더라도 전체 3D 씬이 중단되지 않도록, 개별 모델 단위로 React Error Boundary를 적용합니다. 로딩에 실패한 모델은 빨간색 와이어프레임 큐브 플레이스홀더로 대체 표시되며, 콘솔에 에러 상세가 기록됩니다.

### 7.2 데이터 영속성

* **클라이언트 사이드 저장**: 모든 데이터는 `localStorage`와 `IndexedDB`에 저장됩니다 (백엔드 서버 불필요).
* **프로젝트 레벨 저장**: 장비 모델 커스터마이제이션(섀시 영역, 타입/변형)은 `customModels.json`에 저장되어 Git 버전 관리됩니다.
* **상태 복원**: 브라우저 새로고침 또는 재방문 시 마지막 저장 상태가 자동 복원됩니다.
* **데이터 이관**: 엑셀(`.xlsx`) 기반 Import/Export를 통한 장비 목록 및 전체 환경 데이터 이관을 지원합니다.
* **초기화 보호**: 데이터 초기화 시 장비 모델 관리 데이터(커스텀 모델, 타입/변형, 섀시 영역)는 보호되어 유지됩니다.

### 7.3 브라우저 호환성

* **대상 브라우저**: WebGL 2.0을 지원하는 모던 브라우저 (Chrome, Edge, Firefox, Safari 최신 버전)
* **반응형 대응**: 주 대상은 데스크탑 환경이며, 대형 모니터(풀HD 이상) 사용을 권장합니다.
